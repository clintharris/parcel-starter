import React from 'react';

import styles from '@src/App.scss';

export default class App extends React.Component {
  render() {
    return (
      <div className={styles.root}><h5>Hello, world!</h5></div>
    );
  }
}